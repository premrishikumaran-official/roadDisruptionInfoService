infoServiceProvider:
  url: https://api.tfl.gov.uk
  appId: road-app
  appKey: <tfl dev key> #ToDo for prod

feign:
  hystrix:
    enabled: true
  client:
    config:
      feignName:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: ERROR

