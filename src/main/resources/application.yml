infoServiceProvider:
  url: https://api.tfl.gov.uk
  appId: road-app
  appKey: "ToUpdate"

feign:
  client:
    config:
      feignName:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: full

