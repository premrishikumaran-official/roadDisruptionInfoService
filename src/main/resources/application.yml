infoServiceProvider:
  url: https://api.tfl.gov.uk
  appId: road-app
  appKey: "ToUpdate"

feign:
  hystrix:
    enabled: true
  client:
    config:
      feignName:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic


